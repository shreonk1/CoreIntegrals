# This class can be used to enable and disable commands. For example if /broadcast has enabled: true
# the /broadcast command will work. Otherwise it will not.

Alias:
  #
  /broadcast:
    Enabled: false
  #
  /screenbroadcast:
    Enabled: false
  #
  /clearchat:
    Enabled: false
  #
  /nickname:
    Enabled: false
  #
  /gamem:
    Enabled: false
  #
  /broadcasthelp:
    Enabled: false
  #
  /staffhelp:
    Enabled: false
  #
  /kits:
    Enabled: false
  #
  /editperms:
    Enabled: false
  #
  /staffchat:
    Enabled: false
  #
